apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "contract-ai.fullname" . }}-test-connection"
  labels:
    {{- include "contract-ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "contract-ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
